#SQL STATEMENT OR SQL LANGUAGE #
# DATA DEFINATION LANGUAGE (DDL)
    a.CREATE
    b.RENAME
    c.ALTER
    d.TRUNCATE
    e. DROP
# DATA MANIPULATION LANGUAGE (DML)
     a.INSERT
     b.UPDATE
     c.DELETE
#  TRANSATION CONTROL LANGUAGE (TCL)
      a.COMMIT
      b.SAVEPOINT
      c.ROLL BACK
 # DATA CONTROL LANGUAGE (DCL)
     a.GRANT
     b.REVOKE
# DATA QUERY LANGUAGE (DQL)
     a. SELECT
     b.PROJECTION
     c.SELECTION
     d.JOINS
